# Environment variables declared in this file are validated in src/lib/env.ts
# Only NEXT_PUBLIC_ variables are exposed to the client.

# The canonical URL of your production deployment.
# Used for metadata, sitemaps, and robots.txt.
NEXT_PUBLIC_APP_URL="http://localhost:3000"
